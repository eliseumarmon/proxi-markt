# Iniciar proyecto Vue 🛒🍅

Comprobamos si tenemos node instalado con:

    node -v

Si no lo tenemos instalado hay que hacer:

    npm install node

Una vez esté instalado lo anterior hay que instalar lo siguiente:

    npm install vue-router
    npm install axios

Con todo lo anterior instalado ya podriamos iniciar el proyecto con:

    cd frontend
    npm run dev
